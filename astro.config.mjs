// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://ki11j0yz.github.io', // Your GitHub Pages URL
    base: '/astro_project',             // Your repository name (must have leading slash)
});
